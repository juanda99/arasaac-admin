import { defineMessages } from 'react-intl'

export const scope = 'app.components.ThemeSelector'

export default defineMessages({
  toggleTheme: {
    id: `${scope}.button.darkTheme`,
    defaultMessage: 'Toggle light/dark theme',
  },
})
