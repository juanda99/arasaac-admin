import { defineMessages } from 'react-intl'

export const scope = 'app.containers.CatalogsView'

export default defineMessages({
  catalogGeneration: {
    id: `${scope}.h1`,
    defaultMessage: 'Catalog Generation',
  },
})
