import { defineMessages } from 'react-intl'

export const scope = 'app.components.Catalog'

export default defineMessages({
  catalogGeneration: {
    id: `${scope}.h1`,
    defaultMessage: 'Catalog Generation for {language} language',
  },
})
