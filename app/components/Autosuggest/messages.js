import { defineMessages } from 'react-intl'

export const scope = 'app.components.Autosuggest'

export default defineMessages({
  selectTagsHint: {
    id: `${scope}.selectTagsHint`,
    defaultMessage: 'Select multiple tags',
  },
})
